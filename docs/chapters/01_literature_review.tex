% I. Предметна област и обосновка на проектните решения.
% Обединява предишните раздели „литературен преглед“ и „анализ на алтернативите“.
\section{Предметна област и обосновка на решенията}
\label{sec:literature}

\subsection{Информационен модел, синтаксиси и семантика}

Класическият уеб свързва документи с хипервръзка без тип: тя казва, че два документа са
свързани, но не и как. Семантичният уеб описва ресурсите и дава на всяка връзка собствен
идентификатор и значение. Множество триплети образува насочен етикетиран граф, в който
допълнението на един триплет може да бъде подлог на друг, и така отделно съставени описания се
свързват без обща схема. RDF~1.1 въвежда трите вида термини, тоест IRI, литерал и празен възел,
и разделя абстрактния модел от конкретните му записи~\cite{w3c_rdf11_concepts};
затова анализаторите са сменяеми входове към един вътрешен модел. Turtle е компактният четим
запис~\cite{w3c_turtle}, а N-Triples е негово подмножество с по един пълен триплет на
ред~\cite{w3c_ntriples}.

\begin{lstlisting}[language={},caption={Част от \code{data/small.ttl}}]
@prefix ex:   <http://example.org/> .
@prefix rdfs: <http://www.w3.org/2000/01/rdf-schema#> .

ex:alice a ex:Author ; ex:name "Alice" ; ex:age 34 ;
         ex:knows ex:bob, ex:carol .

ex:Author     rdfs:subClassOf    ex:Person .
ex:mainAuthor rdfs:subPropertyOf ex:author .
\end{lstlisting}

Синтаксисът определя кои записи са допустими, но не и какво следва от тях. Семантиката на RDF
задава кога един граф следва от друг~\cite{w3c_rdf11_semantics}, а RDF Schema надгражда с
речник за класове, свойства, области и обхвати~\cite{w3c_rdf_schema}: от последните два реда на
примера следва, че \code{ex:alice} е и \code{ex:Person}, и че всеки триплет по
\code{ex:mainAuthor} важи и по \code{ex:author}. Езикът OWL~2 добавя ограничения върху
свойствата, кардиналности и дизюнктни класове, и с това променя изчислителната сложност на
извеждането~\cite{w3c_owl2_overview}; проектът реализира RDFS като отделен слой и разглежда
OWL~2 само описателно. SPARQL~1.1 дефинира базови графови шаблони, незадължителни части,
обединения, филтри, агрегация, подзаявки и пътища по свойства, а също и алгебра, към която
текстът се превежда преди изпълнение~\cite{w3c_sparql11_query}; тази алгебра е границата между
анализатора и изпълнението, приета за такава и тук.

Литературата за съхранение на триплети се съсредоточава върху един въпрос: базовият графов
шаблон изисква търсене по различни комбинации от свързани и свободни позиции, а един ред на
сортиране обслужва добре само част от тях. Hexastore предлага пълния набор от шест пермутации,
срещу шесткратно повторение на данните~\cite{weiss2008hexastore}, а RDF-3X показва, че при
речниково кодиране подходът е приложим, и добавя планировчик, който избира реда на съединенията
по статистики върху самите индекси~\cite{neumann2010rdf3x}.

\subsection{Обосновка на направения избор}

\begin{table}[H]
\caption{Разгледани алтернативи и направен избор}
\label{tab:alternatives}
\centering
\fontsize{10}{12}\selectfont
\begin{tabular}{@{}L{2.6cm}L{4.2cm}L{3.0cm}L{3.4cm}@{}}
\toprule
\textbf{Решение} & \textbf{Разгледани варианти} & \textbf{Избор} & \textbf{Водещ довод} \\
\midrule
Език & C++20, Rust, Java & C++20 & вграждане без отделна среда за изпълнение \\
Термини & низове в триплета, речниково кодиране & речниково кодиране & фиксиран размер, евтини сравнения \\
Индекси & един, три, шест пермутации & три пермутации & покритие на всички шаблони без шесткратен обем \\
Носител & памет, външен склад, отразени файлове & редици в паметта & същите алгоритми, без платформено зависим вход и изход \\
Анализатори & генератор, рекурсивно спускане & рекурсивно спускане & по-добри съобщения, без стъпка в изграждането \\
Планировчик & фиксирано правило, оценка по статистики & оценка по статистики & устойчив спрямо реда на изписване \\
\bottomrule
\end{tabular}
\end{table}

Изборите са обобщени в \tabref{tab:alternatives}, а два от тях имат нужда от повече от един ред
довод. \textbf{Носителят:} между подредени редици в паметта и същите редици във файл разликата
е само откъде идват байтовете. Избрана е паметта, защото отразяването във файлове изисква
платформено зависим вход и изход, без да променя алгоритмите; ограничението е записано като
такова, а именно че наборът трябва да се събира в паметта. \textbf{Планировчикът:} при
подредени индекси броят на триплетите, напасващи един шаблон, е дължината на диапазона от
двоичното търсене, тоест пресмята се точно за $O(\log n)$.
